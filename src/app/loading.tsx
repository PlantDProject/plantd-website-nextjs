const Loading = () => {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <div id="globalLoader">
            <img className="loadingAnimation" src="/next-images/favicon.png" alt="" />
        </div>
    );
};

export default Loading;
